<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.shliapp">

    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />

    <uses-feature android:name="android.hardware.location.gps" />

    <application
        android:allowBackup="false"
        tools:replace="android:allowBackup"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@drawable/ic_launcher"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        android:usesCleartextTraffic="true">
        <activity android:name=".Activities.SecondIntroActivity"
            android:theme="@style/Theme.Intro"/>
        <activity android:name=".Activities.FirstIntroActivity" />
        <activity android:name=".Activities.ChangePassActivity" />
        <activity android:name=".Activities.VerifyActivity" />
        <activity android:name=".Activities.ForGotPasswordActivity" />
        <activity android:name=".Activities.CongratulationActivity" />
        <activity android:name=".Activities.AllowLocationActivity" />
        <activity android:name=".Activities.ChooseStoreActivity" />
        <activity android:name=".Activities.AddGroceryActivity" />
        <activity android:name=".Activities.UnderSinkActivity" />
        <activity android:name=".Activities.AddStorageActivity" />
        <activity android:name=".Activities.HomeScreenActivity" />
        <activity android:name=".Activities.StartBottomActivity" />
        <activity android:name=".Activities.SignUpActivity" />
        <activity android:name=".Activities.LoginActivity" />
        <activity android:name=".Activities.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>